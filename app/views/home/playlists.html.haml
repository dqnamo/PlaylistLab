
.container
  .row.pl-4.d-flex.align-items-center
    .col-md-6.ml-0.pl-0
      %h3.login-heading.my-4 🎶 Your Playlists.
    .col-md-6
      = render "header_options"

  .row
    - @playlists.each do |x|
      .col-md-4
        .card.mb-4.bg-light
          .row.no-gutters
            .col-md-4
              %img.card-img{:alt => "...", :src => "#{x.images.first.try(:fetch, "url")}"}/
            .col-md-8
              .card-body
                %h5.card-title
                  = x.name
                  %small.text-muted
                    = "#{x.tracks.count} songs"
                = link_to('🔥 Enhance', playlist_path(x.id), class: "btn btn-sm bg-dark text-white")

  .row
    .card.w-100.ml-3.animated.infinte.pulse.delay-4s
      .row.no-gutters
        .card-body
          .col-md-6
            %h5.card-title.mb-1
              🍀 Feeling Lucky?
            %small.text-muted
              Let us create a recommended playlist for you based on your listening habits.
            = link_to('Make Me A Playlist', '/auth/spotify', class: "btn btn-sm btn-primary mt-4")
